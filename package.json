{
  "name": "phi-dynamic-directives-seed",
  "private": true,
  "version": "0.0.1",
  "description": "A starter project for PHI dynamic directives",
  "devDependencies": {
    "bower": "^1.3.1",
    "shelljs": "^0.2.6",
    "grunt": "~0.4.5"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 1337 && grunt restoreDefaultPhiPayloadData;scripts/web-server.js;"
  }
}
